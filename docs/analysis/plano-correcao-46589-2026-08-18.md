# Plano de correção 46589 — 18/08/2026

> **@aios-master (Orion)** unificando Atlas + Aria + Quinn. Fatia aplicada no código nesta sessão.
> Sem go-live (`BOT_ACCEPT_ALL=false`). Sem Meta/WABA. Sem merge em `main`.

---

## 1. Os agentes certos olharam?

| Quem | Papel | Veredito | O que cobriu |
|---|---|---|---|
| **Atlas (@analyst)** | Diagnóstico do smoke de aderência | Scorecard + kill-rules K1–K8 | `docs/analysis/recepcao-smoke-aderencia-2026-08-18.md` |
| **Orion (@aios-master)** | Pesquisa de mercado / tamanho do prompt | Prompt ~3.5k tokens **não** é a causa; haystack por turno sim | `docs/research/2026-08-18-prompt-framework-whatsapp-reception/` |
| **Aria (@architect)** | Desenho do patch | **CONFIRM-WITH-CHANGES** | Ordem: idempotência sessão **e** DB; guarda de expediente; I.8 copy; trim history ~8; snapshot filter **não** neste sprint |
| **Quinn (@qa)** | Gate de qualidade | **CONFIRM-WITH-CHANGES** | K-det (K1,K2,K3,K5) bloqueiam; K-style (K4,K6,K7,K8) n=2 humano; `isSalonOpen` fora de `server.js`; congelar `test-conversa-v3.mjs` |

**Sim.** O feedback da recepção foi pontuado pelo analista; a hipótese “prompt grande / trocar framework” foi pesquisada pelo orquestrador; o plano de código foi confirmado por arquitetura e QA **antes** desta aplicação.

O que **não** entra nesta fatia (consenso Aria+Quinn+Atlas):

- Reescrever PACER / migrar TESS → Gemini.
- Filtrar snapshot (119 serviços) sem golden-set.
- Decidir atendimento paralelo (Erick ∥ Jackie) — 4 opções em Atlas §7. Trip-wire: 2 profissionais → `[HANDOFF_HUMAN motivo=multi_servico]`.
- Reescrever o scorer `scripts/test-conversa-v3.mjs`.

---

## 2. O que esta fatia aplica (ordenado)

1. **Idempotência de create** — chave `sha1(phone|serviceId|professionalId|date|time)` no `Set` da sessão **e** `listAppointmentsByClient` no snapshot local (`scheduled`/`confirmed`). Duplicata: log `[idempotency]`, sem segundo POST, sem segunda mensagem de sucesso.
2. **Guarda dura de expediente** — `isSalonOpen` + `bookingFitsExpediente` em `backend/lib/salon-dates.js`. Rejeita início fechado **ou** `início+duração` depois do fechamento do mesmo dia (sex 19h, sáb 18h). Duração da tag ou, se faltar, do catálogo.
3. **I.8 copy** — `"Te esperamos"` só se o salão está aberto **agora**. Combo também dispara `afterHoursBooking` (não só `bookingConfirm`).
4. **Trim de histórico** — últimas 8 mensagens de `state.history` e de `persistedMemory.history` injetadas no modelo. `rootId` TESS continua no thread.
5. **Prompt** — ROLE “referência”; I.11; I.12; vocabulário; WhatsApp sem markdown; trip-wire; combo 2 profs → handoff. Colar no TESS via `docs/handoffs/46589-prompt-changes-2026-06-02.md` §7.

---

## 3. Como validar (Quinn)

- `cd backend && npm test` — expediente + idempotência + copy I.8.
- Smoke humano (n=2 no K-style): Dylan recusa só unhas; Tiago STOP depois do preço; 1 bolha; sexta **não** oferece 19:30 nem empilha até 23:40; dado solto (nascimento) **não** recria reserva.

Kill-rules desta fatia: **K1 expediente, K2 duplicata, K3 remarcar em vez de recriar, K5 não sumir serviço confirmado**. K4/K6/K7/K8 melhoram com o prompt; não bloqueiam CI.

---

## 4. Ainda aberto (produto)

Dois profissionais no mesmo horário de início — Atlas §7 opções 1–4. Até Victor/recepção escolherem, o bot escala. Não inventar regra paralela.
