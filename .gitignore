# Environment & Secrets (AIOS)
.env
.env.local
.env.*.local
infra/.env
.cursor/tess-mcp.headers
.cursor/tess-prod.env
*.key
*.pem

# Dependencies (AIOS)
node_modules/
node_modules
**/node_modules/

# Build & Logs (AIOS)
dist/
build/
*.log
logs/
npm-debug.log*
coverage/

# IDE & OS (AIOS)
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# AIOS Local (AIOS)
.aios-core/local/
.claude/settings.local.json
.aios/install-log.txt

# Runtime & Temp (AIOS)
.aios/*.lock
.aios/*.tmp.*
*.backup.*
tmp/
temp/

# Local CLI tools (static ffmpeg, venv)
.tools/

# Raw materials — large binaries (use cloud storage)
docs/raw-materials/

# Agent Handoffs — podem conter credenciais de sessão
.aios/handoffs/
.claude/worktrees/

# AIOX Local (AIOX)
.aiox-core/local/
.aiox/install-log.txt

# Vendor clones used only as install source (do not commit)
.cache/
